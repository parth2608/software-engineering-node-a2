/**
* @file Declares Enumeration for Account type
* defining three account types for the users
*/

enum AccountType {
   Personal = "PERSONAL",
   Academic = "ACADEMIC",
   Professional = "PROFESSIONAL"
};

export default AccountType;